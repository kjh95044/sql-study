SELECT
*
FROM 
(

SELECT
    ANIMAL_TYPE,
    COUNT(*) AS CAT_COUNT
from ANIMAL_INS
WHERE ANIMAL_TYPE = 'Cat'
    
   UNION ALL
    
SELECT
      ANIMAL_TYPE,
    COUNT(*) AS DOG_COUNT
FROM ANIMAL_INS
    WHERE ANIMAL_TYPE = 'Dog'
) AS X
ORDER BY ANIMAL_TYPE ASC
